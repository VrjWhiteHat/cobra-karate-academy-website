import { ArrowUpRight, Trophy } from "lucide-react";
import { Link } from "wouter";
import { trpc } from "@/lib/trpc";
import { Footer } from "./Home";
export default function Achievements() { const { data: content } = trpc.content.get.useQuery(); const items = content?.achievements ?? []; return <div className="site-shell"><Header /><main><section className="page-hero page-width"><div className="page-hero-meta"><span>03</span><span>The record</span></div><h1>Moments that<br /><em>stay with you.</em></h1><p>Every win, lesson, and milestone becomes part of the academy’s living record.</p></section><section className="section page-width record-list">{items.map((item: { year: string; title: string; description: string }, index: number) => <article className="record-card" key={item.title}><div className="record-year">{item.year}</div><div className="record-icon"><Trophy size={22} /></div><div><h2>{item.title}</h2><p>{item.description}</p></div><span>0{index + 1}</span></article>)}</section><section className="section red-band"><div className="page-width record-cta"><div><div className="section-label"><span />Your chapter</div><h2>Make the next<br /><em>one yours.</em></h2></div><Link className="button button-light" href="/contact">Join the work <ArrowUpRight size={16} /></Link></div></section></main><Footer content={content} /></div>; }
function Header() { return <header className="site-header page-width"><Link href="/" className="brand-lockup"><img src="/manus-storage/cobra-logo_d1e71238.png" alt="Cobra crest" /><span><small>THE</small> COBRA</span></Link><nav className="desktop-page-nav"><Link href="/academy">Academy</Link><Link href="/training">Training</Link><Link href="/gallery">Gallery</Link><Link className="nav-cta" href="/contact">Start here <ArrowUpRight size={14} /></Link></nav></header>; }
